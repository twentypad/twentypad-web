import {getTokens} from "@/lib/supabase/queries";import {TokenList} from "@/components/token-list";import {ClaimFees} from "@/components/claim-fees";import {shortAddress} from "@/lib/format";export default async function Creator({params}:{params:Promise<{address:string}>}){const {address}=await params;const tokens=await getTokens({creator:address});return <div className="space-y-7"><div><span className="pill">Creator</span><h1 className="mt-3 text-3xl font-bold">{shortAddress(address)}</h1><p className="mt-2 text-twenty-muted">Launches and quote-asset fees credited by the TwentyPad fee escrow.</p></div><ClaimFees/><section><h2 className="mb-3 text-xl font-semibold">Launches ({tokens.length})</h2><TokenList tokens={tokens}/></section></div>}
