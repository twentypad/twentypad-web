import {NextRequest,NextResponse} from "next/server";import {indexFactory} from "@/lib/indexer/sync";export const runtime="nodejs";export async function POST(req:NextRequest){const expected=process.env.INDEXER_SECRET;if(!expected||req.headers.get("authorization")!==`Bearer ${expected}`)return NextResponse.json({error:"Unauthorized"},{status:401});try{return NextResponse.json(await indexFactory())}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Indexer failed"},{status:500})}}
