import {NextRequest,NextResponse} from "next/server";import {isHash} from "viem";import {indexReceipt} from "@/lib/indexer/sync";export const runtime="nodejs";export async function POST(req:NextRequest){try{const body:unknown=await req.json();if(!body||typeof body!=="object"||!("tx" in body)||typeof body.tx!=="string"||!isHash(body.tx))return NextResponse.json({error:"Invalid transaction hash"},{status:400});const token=await indexReceipt(body.tx);return NextResponse.json({token})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Index failed"},{status:500})}}
