"use client";import {useAccount} from "wagmi";import {useRouter} from "next/navigation";import {useEffect} from "react";export default function My(){const {address}=useAccount();const router=useRouter();useEffect(()=>{if(address)router.replace(`/creators/${address}`)},[address,router]);return <div className="card mx-auto max-w-lg p-8 text-center">{address?"Opening your creator profile…":"Connect your creator wallet, then return here."}</div>}
