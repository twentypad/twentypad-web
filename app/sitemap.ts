import type {MetadataRoute} from "next";import {getTokens} from "@/lib/supabase/queries";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base="https://twentypad.com",staticPages=["","/create","/swap","/guide","/about","/privacy","/terms"].map(path=>({url:base+path,lastModified:new Date()}));const tokens=await getTokens({limit:1000});return[...staticPages,...tokens.map(t=>({url:`${base}/token/${t.address}`,lastModified:new Date(t.launched_at)}))]}
