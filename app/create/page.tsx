import type {Metadata} from "next";import {CreateForm} from "@/components/create-form";export const metadata:Metadata={title:"Create a B20"};export default function Create(){return <div><div className="mb-7"><span className="pill text-twenty-ca7">Every TwentyPad B20 is a ca7.</span><h1 className="mt-4 text-3xl font-bold">Launch a B20</h1><p className="mt-2 text-twenty-muted">One transaction creates the token, opens the pool, seeds token-only liquidity, and locks it.</p></div><CreateForm/></div>}
