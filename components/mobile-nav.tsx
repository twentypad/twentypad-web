"use client";import Link from "next/link";import {Compass,PlusCircle,ArrowLeftRight,BookOpen} from "lucide-react";import {EXTERNAL} from "@/lib/chain";export function MobileNav(){return <nav className="fixed bottom-0 z-30 grid w-full grid-cols-4 border-t border-twenty-line bg-twenty-navy/95 p-2 md:hidden">{[[Compass,"Discover","/",false],[PlusCircle,"Create","/create",false],[ArrowLeftRight,"Swap","/swap",false],[BookOpen,"Docs",EXTERNAL.docs,true]].map(([I,n,h,external])=>{const Icon=I as typeof Compass;const className="flex flex-col items-center gap-1 text-[11px] text-twenty-muted";return external?<a href={h as string} target="_blank" rel="noreferrer" key={n as string} className={className}><Icon size={20}/>{n as string}</a>:<Link href={h as string} key={n as string} className={className}><Icon size={20}/>{n as string}</Link>})}</nav>}
