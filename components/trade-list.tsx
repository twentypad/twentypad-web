import type {Trade} from "@/lib/types";import {age,shortAddress} from "@/lib/format";export function TradeList({trades}:{trades:Trade[]}){if(!trades.length)return <div className="card p-8 text-center text-sm text-twenty-muted">No indexed trades yet.</div>;return <div className="card overflow-x-auto"><table className="w-full text-sm"><tbody>{trades.map(t=><tr className="border-b border-twenty-line last:border-0" key={t.id}><td className={`px-4 py-3 font-medium ${t.side==="buy"?"text-twenty-success":"text-twenty-danger"}`}>{t.side.toUpperCase()}</td><td className="px-4">{t.amount_token} token</td><td className="px-4 text-twenty-muted">{t.amount_quote} quote</td><td className="px-4 text-twenty-muted">{shortAddress(t.trader)}</td><td className="px-4 text-right text-twenty-muted">{age(t.timestamp)}</td></tr>)}</tbody></table></div>}
